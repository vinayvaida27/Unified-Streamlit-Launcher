# Build Folder

Generated build output belongs here.

The release script writes the portable launcher folder to:

```text
build/Unified-Streamlit-Launcher/
```

Do not edit generated build files by hand. Edit source files, app folders, or `apps/apps.json`, then rebuild.
