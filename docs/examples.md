# Examples

The repository includes ten simple demo apps under `apps/`.

Use `apps/app_template` as the starting point for new apps.

Each demo app is intentionally small:

```python
import streamlit as st

st.title("Example")
st.write("Hello")
```
