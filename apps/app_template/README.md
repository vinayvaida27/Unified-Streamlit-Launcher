# App Template

Copy this folder to create a new Streamlit app.

Recommended steps:

1. Rename the copied folder, for example `11_ab1file_process`.
2. Edit `app.py`.
3. Add any extra packages to `requirements.txt`.
4. Replace `assets/icon.svg`.
5. Add the app entry to `apps/apps.json`.
