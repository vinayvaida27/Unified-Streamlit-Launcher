# Security Checklist

- Network release folder is read-only for normal users.
- Checksums are generated and verified.
- Runtime is approved and scanned.
- Release executable is code signed.
- Logs do not contain secrets.
- User output directories are never deleted by cleanup scripts.
